version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pyenv install -s $(cat .python-version)
  build:
    commands:
      - set -e
      - ./scripts/manage_users.sh
